//objects : store data and functions 
//key:value PAIRS
//can add functions, arrays 
//greeting is function
//object.value
//'this' means current object

/*let person = {
    name: "Zee",
    age: 21,
    isVegan: true,
    mood: 'bad',
    faveDrinks: ["Peppermint Tea", "Earl Grey", "Irn Bru"],
    greeting () {
        if (this.mood == "good") {
        return "Hey, good to see you!"}
    else {
        return "**** off!"}
    },
    sayHi () {
        return `Hi there, ${this.name}`
    }
}

//console.log(person.name)
console.log(person['name']);
console.log(person.faveDrinks[1]);

person['favePokemon'] = ['gloom', 'koffing'];
console.log(person.favePokemon);

person.faveSongs = ["Spitting Image - Freelance Whales", "Last of the American Girls - Green Day"];
console.log(person);

console.log(person.sayHi())*/

/*let rabbit = {
    name: "Sherlock",
    colour: "black and white",
    ears: "uppy",
    action: "biting",
    bunIs () {
        return `${this.name} is ${this.action}`
    }
}
console.log(rabbit.bunIs())*/

let noughtsAndCrosses